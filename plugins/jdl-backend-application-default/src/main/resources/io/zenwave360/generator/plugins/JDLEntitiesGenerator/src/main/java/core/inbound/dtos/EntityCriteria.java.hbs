package {{basePackage}}.core.inbound.dtos;

import {{basePackage}}.core.domain.*;
import java.io.Serializable;
import javax.validation.constraints.*;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.Sort;

/**
 *
 */
public class {{entity.className}}{{criteriaDTOSuffix}} extends PageRequest implements Serializable, Pageable {

    public {{entity.className}}{{criteriaDTOSuffix}}() {
        this(0, 10, Sort.unsorted());
    }

    public {{entity.className}}{{criteriaDTOSuffix}}(int page, int size, Sort sort) {
        super(page, size, sort);
    }
}