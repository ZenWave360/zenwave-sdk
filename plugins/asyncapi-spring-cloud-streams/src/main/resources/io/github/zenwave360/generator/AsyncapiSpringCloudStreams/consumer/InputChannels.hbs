package {{api.apiPackage}};

import org.springframework.cloud.stream.annotation.Input;
import org.springframework.messaging.SubscribableChannel;

public interface {{interfaceClassName}}InputChannels {

<#list api.operations as operation>
    String {{operation.operationId?upper_case?replace("-","_")} = "${operation.operationIdKebabCase}}";

</#list>

<#list api.operations as operation>
    @Input({{operation.operationId?upper_case?replace("-","_")}})
    SubscribableChannel {{operation.operationId}}();

</#list>

}
