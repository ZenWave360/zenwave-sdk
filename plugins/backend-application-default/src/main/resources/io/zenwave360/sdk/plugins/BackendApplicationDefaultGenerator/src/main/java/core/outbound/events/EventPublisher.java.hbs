package {{layout.outboundEventsPackage}};

import {{layout.domainEventsPackage}}.*;
import {{layout.entitiesPackage}}.*;

public interface EventPublisher {

{{#each events as |event|}}
    void on{{event.className}}({{event.className}} event);
{{/each}}
}
