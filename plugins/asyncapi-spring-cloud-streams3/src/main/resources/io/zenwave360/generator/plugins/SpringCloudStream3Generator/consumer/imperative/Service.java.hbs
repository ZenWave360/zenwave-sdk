package {{apiPackage}};

{{#if modelPackage}}
import {{modelPackage}}.*;
{{/if}}

/**
* {{asyncapi.description}}
*/
public interface {{operation.x--operationIdCamelCase}}{{serviceSuffix}} {

{{#each operation.x--messages as |message|}}
    /**
     * {{operation.description}}
     */
    boolean {{operation.operationId}}{{#if (size operation.x--messages) eq 1}}_{{message.name}}{{/if}}({{message.x--javaType}} msg);
{{/each}}

}