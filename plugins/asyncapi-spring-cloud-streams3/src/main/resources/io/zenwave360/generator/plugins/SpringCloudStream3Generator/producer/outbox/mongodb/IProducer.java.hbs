package {{apiPackage}};

{{#if modelPackage}}
import {{modelPackage}}.*;
{{/if}}

/**
 * {{asyncapi.description}}
 */
public interface I{{apiClassName}} {

{{#each operations as |operation|}}
    {{#each operation.x--messages as |message|}}
    /**
     * {{operation.description}}
     */
    boolean {{operation.operationId}}({{message.x--javaType}} payload, Header... headers);
    {{/each}}
{{/each}}
}
