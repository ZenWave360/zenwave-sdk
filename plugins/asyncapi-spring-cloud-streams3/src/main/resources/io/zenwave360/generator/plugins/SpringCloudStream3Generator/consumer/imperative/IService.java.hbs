package {{apiPackage}};

import java.util.Map;
{{#if modelPackage}}
import {{modelPackage}}.*;
{{/if}}

/**
* {{asyncapi.description}}
*/
public interface {{serviceName operation.x--operationIdCamelCase}} {

{{#each operation.x--messages as |message|}}
    /**
     * {{operation.description}}
     */
    {{~#if exposeMessage}}
    default void {{operation.operationId}}{{methodSuffix message operation}}({{message.x--javaType}} msg) {};
    {{else}}
    default void {{operation.operationId}}{{methodSuffix message operation}}({{message.x--javaType}} payload, Map<String, Object> headers) {};
    {{/if}}
{{/each}}

}
