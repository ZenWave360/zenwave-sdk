package {{producerApiPackage}};

{{#if modelPackage}}
import {{modelPackage}}.*;
{{/if}}

/**
 * {{asyncapi.description}}
 */
@javax.annotation.Generated(value = "io.zenwave360.sdk.plugins.SpringCloudStreams3Plugin", date = "{{date}}")
public interface I{{apiClassName}} {

{{#each operations as |operation|}}
    {{#each operation.x--messages as |message|}}
    {{~> (partial '../common/Headers') isProducer=true}}
    /**
     * {{{operation.summary}}}
     */
    boolean {{operation.operationId}}({{message.x--javaType}} payload, {{message.x--javaTypeSimpleName}}Headers headers);
    default boolean {{operation.operationId}}({{message.x--javaType}} payload) {
        return {{operation.operationId}}(payload, null);
    };
    {{/each}}
{{/each}}
}
