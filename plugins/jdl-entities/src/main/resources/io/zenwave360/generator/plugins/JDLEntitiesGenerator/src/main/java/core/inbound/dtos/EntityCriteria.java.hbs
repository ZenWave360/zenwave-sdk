package {{basePackage}}.core.inbound.dtos;

import {{basePackage}}.core.domain.*;
import java.io.Serializable;
import javax.validation.constraints.*;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.Sort;

/**
 *
 */
public class {{entity.className}}Criteria extends PageRequest implements Serializable, Pageable {

    public {{entity.className}}Criteria() {
        this(0, 10, null);
    }

    public {{entity.className}}Criteria(int page, int size, Sort sort) {
        super(page, size, sort);
    }
}