ZW> SDK ({{version}})

Available plugins:

{{#each plugins as |plugin| ~}}
{{{plugin.configClassName}}}: {{{plugin.plugin.title}}} ({{plugin.version}})
{{/each}}

Use: "jbang zw -p <plugin | short-code> -h" to get help on a specific plugin
